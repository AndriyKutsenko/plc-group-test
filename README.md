
Начальные значения

```
php artisan migrate:fresh --seed

// или

php artisan migrate
php artisan db:seed
```

Один из вариантов запуска сервера (без nginx / apache / и т.д.)
```
php artisan serve
```
